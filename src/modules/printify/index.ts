import PrintifyModule from "./module"
import PrintifyModuleService from "./service"

export const PRINTIFY_MODULE = "printify"

// Export the service class for type imports
export { PrintifyModuleService }

// Default export is the full module (includes linkable definitions)
export default PrintifyModule
